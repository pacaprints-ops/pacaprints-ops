Good morning! Let's get the day started.

1. Run `hostname` in bash to identify who is in session. Check CLAUDE.md for the hostname-to-user mapping and greet them by name.

2. Run `git pull` in the project root to fetch the latest changes from the remote repository.

3. If the folder `projects/meal-planner` exists, run `git pull` inside it too to fetch the latest changes.

4. Report back:
   - Who is logged in (by name, based on hostname)
   - Whether the pull brought in any new commits (if yes, briefly summarise what changed)
   - A short "all good, ready to go" confirmation if nothing new

Keep the response short and friendly.
